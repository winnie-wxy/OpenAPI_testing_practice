[pytest]
markers =
    smoke: Quick health and basic CRUD checks
    regression: Full coverage including edge cases and negative tests
    contract: Response schema validation tests
testpaths = tests
addopts = -v --tb=short
